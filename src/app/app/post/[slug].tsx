import { View, Text } from "react-native";
import React from "react";

type Props = {};

const Post = (props: Props) => {
  return (
    <View>
      <Text>Post</Text>
    </View>
  );
};

export default Post;
